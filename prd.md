# Product Requirements

The product is a deterministic, evidence-driven Telegraph Miner for selected official hackathon intents. Quality takes priority over intent count. The current target intent is `AI_TEXT_DETECTION`; `URL_SCAN` remains a measured candidate and is not claimed as competitive until its recall improves. Unsupported catalog intents and the excluded `FRAUD_DETECTION` intent are outside scope.

